# Micro Frontend with SolidJS

This project is an example for micro frontend with solidjs. "Host Project" retrieves a Counter from "Remote Project". 

You need to run code in the below for both folders.

    yarn && yarn start